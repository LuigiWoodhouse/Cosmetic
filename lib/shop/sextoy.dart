import 'package:flutter/material.dart';

class SexToysPage extends StatelessWidget {
  const SexToysPage({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Sex Toys')),
      body: Center(child: Text('Sex Toys Page')),
    );
  }
}
